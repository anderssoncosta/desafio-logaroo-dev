import { Text, View } from "react-native";
import { Container, Content, Title } from "./style";

const Header = () => {
  return (
    <Container>
      <Content>
        <Title>Logaroo</Title>
      </Content>
    </Container>
  );
};

export default Header;
